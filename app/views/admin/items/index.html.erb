<h2>商品一覧</h2>





     <table>
        <thead>
          <tr>
            <th>商品id</th>
            <th>商品名</th>
            <th>税抜価格</th>
            <th>ジャンル</th>
            <th>ステータス</th>
          </tr>
        </thead>
        <tbody>
      <% @items.each do |item|%>
          <tr>
            <td><p><%= item.id %></p>
            </td>
            <td><p><%= link_to item.name, admin_items_path(item)%></p>
            </td>
             <td><p><%= book.price%></p>
            </td>
            <td><p><%= item.genre %></p>
            </td>
          </tr>
        </tbody>
      </table>
    <% end %>