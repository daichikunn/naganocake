<h1><strong>商品一覧</strong></h1><h3>(全x件)</h3>


<div class= "items_box">
  <div class= "items">
    <%= @items.each do |item| %>
    <%= link_to item_path(item.id) do %>
      <%= image_tag item.image %>
    <% end %>
    <!--インラインメゾットをつかう-->
  </div>

</div>